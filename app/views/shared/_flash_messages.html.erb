<% flash.each do |type, message| %>
  <div class="alert <%= bootstrap_class_for(type) %> fade show text-center">
    <button class="close" data-dismiss="alert" aria-label="Close">
        <span aria-hidden="true">&times;</span>
    </button>
    <%= message %>
  </div>
<% end %>