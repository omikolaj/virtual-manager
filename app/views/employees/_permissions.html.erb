<%= form_tag("/permissions", method: "post") do %>
<%= select_tag "employee_id", options_from_collection_for_select(@employees, "id", "name")%>
<%= select_tag "permission_id",options_from_collection_for_select(Permissions::PERMISSIONS, :last, :first)%>
<%= submit_tag "Update"%>
<% end%>